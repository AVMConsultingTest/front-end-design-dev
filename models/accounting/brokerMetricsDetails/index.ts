export interface BrokerMetricsDetails {
    id: number;
    avg_days_to_pay_curr_month: number
    loads_volume_curr_month: number
    payout_volume_change_per: number
    avg_days_to_pay_change_per: number
    broker_id: number
    payout_volume_prev_month: number
    updated_date: string
    loads_volume_prev_month: number
    loads_volume_change_per: number
    avg_days_to_pay_prev_month: number
    payout_volume_curr_month: number
  }
